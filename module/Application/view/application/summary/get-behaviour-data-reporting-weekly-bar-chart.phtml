<div id="weekly-behaviour-data-container" style="width:100%;height: 400px; margin: 0 auto"></div>
<script>
  Highcharts.chart('weekly-behaviour-data-container', {
    chart: {
        type: 'column'
    },
    title: {
        text: ''
    },
    subtitle: {
        text: ''
    },
    exporting:{
        chartOptions:{
            subtitle: {
                text:'Weekly Behaviour Data Entered Online',
            }
        }
    },
    credits: {
        enabled: false
    },
    xAxis: {
        type: 'category',
        labels: {
            rotation: -45,
            style: {
                fontSize: '13px',
                fontFamily: 'Verdana, sans-serif'
            }
        }
    },
    yAxis: {
        min: 0,
        allowDecimals: false,
        title: {
            text: 'No. of behaviour data'
        }
    },
    legend: {
        enabled: false
    },
    tooltip: {
        pointFormat: 'No. of data : <b>{point.y}</b>'
    },
    series: [{
        name: 'No. of behaviour data',
        color: "#C6C6C7",
        data: [
            <?php
            if(isset($response['week']) && count($response['week']) > 0){
                for($i=0;$i<count($response['week']);$i++){
                    if(isset($response['week'][$i])){
            ?>
            ['<?php echo $response['week'][$i]; ?>', <?php echo $response['total'][$i]; ?>],
            <?php } } } ?>
        ],
        dataLabels: {
            enabled: true,
            rotation: -90,
            color: '#FFFFFF',
            align: 'right',
            format: '{point.y}', // one decimal
            y: 10, // 10 pixels down from the top
            style: {
                fontSize: '13px',
                fontFamily: 'Verdana, sans-serif'
            }
        }
    }]
 });
</script>